import { describe, it } from 'vitest';

describe('env', () => {
    it('should have VITE_API_BASE_URL', () => {
        // todo: fix this test
        // expect(API_BASE_URL).toBeDefined();
    });
});
